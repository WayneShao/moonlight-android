# RayNeo 12.2 Implementation Plan

**Goal:** migrate to latest Moonlight, repair stereo and native focus, default to 720p30.

**Architecture:** isolated classes under com.limelight.ui.rayneo; Application registers common window adaptation; Game retains its existing decoder/input APIs through an explicit SurfaceHolder bridge to a separate OES renderer.

**Tech Stack:** Java 17, Android framework Views, AGP 9.4, Gradle 9.7.1, NDK 29.

- [x] Fetch latest official branch, preserve original dirty files, fast-forward isolated adaptation branch, update recursive submodules.
- [x] Add executable pure-Java regression tests for identity, fit, tap/swipe/cancel/double-click policy; observe failure then implement policy.
- [x] Replace legacy helper/layout with isolated device, window host, focus and gesture components. Register from Application; remove obsolete UiHelper/HelpActivity hooks.
- [x] Replace Game's inflated stream output before input setup, implement OES SurfaceHolder lifecycle and aspect ratio, preserve upstream callbacks and standard-device path.
- [x] Initialize requested 720p30 preset once; preserve subsequent preferences. Cover in-window messages.
- [x] Review window creation inventory and delayed-input cancellation. Review diff against b48494cb, verify no unrelated changes.
- [x] Build `:app:assembleNonRootDebug :app:lintNonRootDebug`, run focused regressions. Verify APK signature, manifest, ABI and hash; record hardware boundaries.

Source changes are reviewed before delivery. Device installation, launch and physical acceptance are distinct states and are not implied by these host checks.
